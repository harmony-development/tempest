{
  "auth": {
    "invalid-email": "Invalid email",
    "already-registered": "Email already registered",
    "invalid-password": "Invalid password"
  },
  "register": {
    "password-length": "Password must be between {{ minLength }} and {{ maxLength }}",
    "password-policy": "Password must have at least: {{ minLower }} lowercase, {{ minUpper }} uppercase, {{ minNumbers }} numbers, and {{ minSpecial }} special characters"
  }
}
